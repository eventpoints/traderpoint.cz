<span {{ attributes.defaults({ class: this.classes }) }}>
    {% if icon %}
        <i class="{{ icon }} me-1"></i>
    {% endif %}
    {% if text is defined  %}
        {{ text }}
    {% endif %}
</span>
