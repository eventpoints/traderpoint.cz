{% extends 'base.html.twig' %}

{% block title %}Trader Point – Get more jobs, less hassle{% endblock %}

{% block body %}
    <!-- HERO -->
    <div class="card text-bg-dark border-0 rounded-0 mb-3">
        <img class="img-fluid w-100 object-fit-cover card-img card-overlay-image card-img-overlay-filter"
             style="height:80vh"
             src="{{ asset('images/tradersmen.jpg') }}"
             alt="Photo by Heiko Ruth: https://www.pexels.com/photo/plumber-repairing-power-source-7859953/">
        <div class="card-img-overlay d-flex flex-column justify-content-center align-items-center text-center">
            <div class="display-5 display-md-3 text-white fw-bold">{{ 'get-jobs-you-want-fast'|trans }}</div>
            <p class="lead mt-2 mb-4 text-white-50">{{ 'landing-explainer-short'|trans }}</p>
            <div class="d-flex gap-2 flex-wrap justify-content-center">
                <a href="{{ path('trader_register') }}"
                   class="btn btn-primary btn-lg px-4">{{ 'join-as-trader'|trans }}</a>
                <a href="{{ path('app_login') }}" class="btn btn-outline-light btn-lg px-4">{{ 'login'|trans }}</a>
            </div>
            <div class="mt-3 small text-white-50">{{ 'join-tagline'|trans }}</div>
        </div>
    </div>

    <div class="container">
        <div class="row g-3 justify-content-center">
            <div class="col-12 col-md-10">
                <div class="row g-3 justify-content-between">
                    <div class="col-12 col-md-4">
                        <div class="card h-100 border-0 rounded-0 mb-3">
                            <div class="card-body">
                                <div class="display-3 text-primary">1.</div>
                                <div class="text-center">
                                    <div class="fs-4 mb-2">{{ 'trader-find-work'|trans|title }}</div>
                                    <p class="card-text">{{ 'trader-find-work-explainer'|trans }}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="card h-100 border-0 rounded-0 mb-3">
                            <div class="card-body">
                                <div class="display-3 text-primary">2.</div>
                                <div class="text-center">
                                    <div class="fs-4 mb-2">{{ 'trader-send-quote'|trans|title }}</div>
                                    <p class="card-text">{{ 'trader-send-quote-explainer'|trans }}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="card h-100 border-0 rounded-0 mb-3">
                            <div class="card-body">
                                <div class="display-3 text-primary">3.</div>
                                <div class="text-center">
                                    <div class="fs-4 mb-2">{{ 'trader-complete-job'|trans|title }}</div>
                                    <p class="card-text">{{ 'trader-complete-job-explainer'|trans }}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
