<nav class="navbar navbar-expand-lg bg-light mb-3" data-bs-theme="light">
    <div class="container">
        <a class="navbar-brand" href="#"><span class="fw-bolder">Trader</span> <span
                    class="fw-lighter">point</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03"
                aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav ms-auto align-items-center">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
                           aria-haspopup="true" aria-expanded="false">{{ app.user.name }}</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item"
                               href="{{ path('user.dashboard') }}">{{ 'dashboard'|trans }}</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item text-danger"
                               href="{{ path('logout') }}">{{ 'app_logout'|trans }}</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">{{ 'auth.login'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">{{ 'auth.register'|trans }}</a>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        {{ app.locale|upper }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item {{ app.locale == 'cs' ? 'active' }}"
                               href="{{ path('services', {_locale: 'cz'}) }}">{{ 'cs'|locale_name|title }}</a></li>
                        <li><a class="dropdown-item {{ app.locale == 'en' ? 'active' }}"
                               href="{{ path('services', {_locale: 'en'}) }}">{{ 'en'|locale_name|title }}</a></li>
                        <li><a class="dropdown-item {{ app.locale == 'ru' ? 'active' }}"
                               href="{{ path('services', {_locale: 'ru'}) }}">{{ 'ru'|locale_name|title }}</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
